# JS-Bucket-vs-Regular-Sort
Comparison of bucket sorting to rough sort an array (only fine sorting what's needed) vs a standard js sort

To run:

1. npm install

2. gulp

3. File saved to results.csv
